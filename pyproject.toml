[project]
name = "polymarket-radar"
version = "0.0.2"
description = "Skill assets and runtime tool for collecting Polymarket radar summaries"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "requests>=2.32.5",
]
